from vkbottle import Keyboard, KeyboardButtonColor, Text, Callback

# KEYBOARD = Keyboard(inline=True)
# KEYBOARD.add(Text("Кнопка"), color=KeyboardButtonColor.POSITIVE)
# KEYBOARD.add(Callback("button", {"cmd": "button"}), color=KeyboardButtonColor.POSITIVE)
# KEYBOARD = KEYBOARD.get_json()

# LANGUAGES = [
#     ап
# ]